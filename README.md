# Dotfiles repository
Managed with stow

Install with
cat pkgs.txt | xargs sudo zypper install

Uninstall other packages:
alacritty -> Replaced with kitty
MozillaFirefox -> Prefer flatpak because of rollback
